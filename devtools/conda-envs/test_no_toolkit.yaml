name: test-no-toolkit
channels:
  - conda-forge
dependencies:
  - python
  - pip
  - pytest
  - pytest-cov
  - pytest-xdist
  # Pull in for consistency with main testing env, will be uninstalled
  - openff-forcefields
