name: test

channels:
  - conda-forge
  - omnia

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - openmm
  - openmmforcefields

    # Standard dependencies
  - openforcefield
